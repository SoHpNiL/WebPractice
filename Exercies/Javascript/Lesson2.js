

const {course} = props;
// a curly braces within event headers = Destructuring
// Creates a new variable called: course
// The data/value of course is found by checking within props to see if there is any assigned data matching course name
// eg. props.course